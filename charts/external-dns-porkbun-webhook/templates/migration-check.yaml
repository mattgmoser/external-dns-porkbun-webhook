{{- if and .Release.IsUpgrade (or (hasKey .Values "legacyStandalone") (hasKey .Values "porkbun")) -}}
{{- fail "chart 0.3.0 and earlier used a standalone webhook topology and cannot be upgraded in place safely; preserve TXT ownership settings, remove the legacy release, and follow the 0.4.0 migration guide" -}}
{{- end -}}
